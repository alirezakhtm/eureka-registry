package com.khtm.eureka;

import com.khtm.eureka.impl.EurekaService;
import com.khtm.eureka.model.HttpResponse;
import org.junit.Assert;
import org.junit.Test;

import java.io.IOException;
import java.util.HashMap;
import java.util.Map;

public class HttParsing {

    @Test
    public void parsingParametersOfRequest() throws IOException {
//        String url = "http://www.google.com/";
//        Map<String, String> parameters = new HashMap<>();
//        parameters.put("q", "iran");
//        String ans = new EurekaService().addRequestParameterToUrl(url, parameters);
//        System.out.println(ans);
//        Assert.assertEquals("http://www.google.com/?q=iran", ans);
//        HttpResponse httpResponse = new EurekaService().sendGetRequest(url, parameters);
//        Assert.assertEquals(httpResponse.getResponseCode(), 200);
//        System.out.println(new EurekaService("", 0).createCrazyString(10));
    }

}
